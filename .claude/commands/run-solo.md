# run-solo

Run the project solo (single agent implements everything phase by phase).

## Usage

Arguments: `<project_name> "<project goal>"`

Run: `bash scripts/run.sh $ARGUMENTS`

Replaces the multi-agent team with a single Claude session that covers all phases.
Useful for small projects or quick prototyping.
